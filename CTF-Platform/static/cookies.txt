<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AetherCorp - Secure Employee Portal</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap');
    
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Roboto', sans-serif;
      background: linear-gradient(135deg, #0a0f1c 0%, #1a2338 100%);
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #e0e7ff;
    }
    .container {
      background: rgba(15, 23, 42, 0.98);
      padding: 45px 55px;
      border-radius: 12px;
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.7);
      width: 100%;
      max-width: 400px;
      border: 1px solid #4b5563;
    }
    .logo {
      text-align: center;
      margin-bottom: 35px;
    }
    .logo h1 {
      font-size: 26px;
      font-weight: 700;
      letter-spacing: 2px;
      color: #60a5fa;
    }
    .logo span {
      font-size: 12px;
      color: #94a3b8;
      display: block;
    }
    h2 {
      text-align: center;
      margin-bottom: 30px;
      color: #cbd5e1;
      font-weight: 500;
    }
    .form-group {
      margin-bottom: 22px;
    }
    label {
      display: block;
      margin-bottom: 6px;
      font-size: 14px;
      color: #94a3b8;
    }
    input {
      width: 100%;
      padding: 13px 15px;
      background: #111827;
      border: 1px solid #374151;
      border-radius: 6px;
      color: #e0e7ff;
      font-size: 15px;
    }
    input:focus {
      outline: none;
      border-color: #60a5fa;
    }
    button {
      width: 100%;
      padding: 14px;
      background: #3b82f6;
      color: white;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      font-weight: 500;
      cursor: pointer;
      margin-top: 10px;
    }
    button:hover {
      background: #2563eb;
    }
    .footer {
      text-align: center;
      margin-top: 30px;
      font-size: 12px;
      color: #64748b;
    }
    .error {
      color: #f87171;
      text-align: center;
      margin: 15px 0;
      display: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
      <h1>AETHERCORP</h1>
      <span>SECURE SYSTEMS DIVISION</span>
    </div>
    
    <h2>Employee Access Portal</h2>
    
    <form id="loginForm">
      <div class="form-group">
        <label for="username">Username</label>
        <input type="text" id="username" placeholder="employee@aethercorp.com" required>
      </div>
      
      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" placeholder="Enter your password" required>
      </div>
      
      <button type="submit">Login to Dashboard</button>
    </form>
    
    <div id="errorMsg" class="error">Invalid credentials. Access denied.</div>
    
    <div class="footer">
      © 2026 AetherCorp Secure Systems • All access logged<br>
      <small>Internal use only</small>
    </div>
  </div>

  <script>
    const form = document.getElementById('loginForm');
    const errorMsg = document.getElementById('errorMsg');

    // Default credentials for normal login (you can change)
    const validUsers = {
      "employee@aethercorp.com": "Aether2026",
      "guest@aethercorp.com": "guest123"
    };

    form.addEventListener('submit', function(e) {
      e.preventDefault();
      
      const username = document.getElementById('username').value.trim();
      const password = document.getElementById('password').value;

      if (validUsers[username] && validUsers[username] === password) {
        errorMsg.style.display = 'none';
        
        // Create the vulnerable auth cookie
        const userData = {
          username: username,
          role: "user",
          is_admin: false,
          clearance: "level3",
          message: "Welcome to the portal - normal user access only"
        };
        
        const encoded = btoa(JSON.stringify(userData));   // plain base64 (easy to decode)
        
        // Set the cookie
        document.cookie = `session_auth=${encoded}; path=/; max-age=3600; SameSite=Lax`;
        
        alert("✅ Login successful!\n\nAuth cookie set: session_auth\n\nHint for next step: Check your cookies and try decoding the value 😉");
        
        // Optional: auto-redirect to a fake dashboard
        // window.location.href = "dashboard.html";
      } else {
        errorMsg.style.display = 'block';
      }
    });

    // Bonus: Show current cookie on page load (for testing)
    window.onload = () => {
      if (document.cookie.includes('session_auth')) {
        console.log("%cCurrent session_auth cookie: " + document.cookie.split('session_auth=')[1].split(';')[0], "color: #60a5fa");
      }
    };
  </script>
</body>
</html>